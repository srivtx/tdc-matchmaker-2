# Primary AI provider — extremely fast (0.5s responses), 1,000 req/day free
# Sign up at https://console.groq.com → API Keys
NEXT_PUBLIC_GROQ_API_KEY=gsk_...

# Fallback AI provider — used if Groq is unavailable
# Sign up at https://openrouter.ai → API Keys
NEXT_PUBLIC_OPENROUTER_API_KEY=sk-or-v1-...
